// =============================================================================
// ЗАГОТОВКА. Точка входа headless-сервера (Linux / Raspberry Pi 5).
// Реализация — Фаза 7.
// =============================================================================
//
// Сервер БЕЗ окна, рендера, аудио и ввода. Только:
//   - authoritative-состояние мира (shared/world),
//   - игровой тик с фиксированным шагом (shared/time/Timer как источник времени),
//   - приём ввода от клиентов и рассылка снапшотов (shared/net + server/net).
//
// Собирается GCC под ARM64. Не линкуется ни с чем Windows-специфичным:
// сюда не должны попадать <windows.h>, GDI, Winsock напрямую — только через
// общий socket-интерфейс из shared/net.
//
// int main() { /* цикл сервера: PollNetwork -> StepSimulation(dt) -> Broadcast */ }