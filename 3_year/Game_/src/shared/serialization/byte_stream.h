#pragma once

// =============================================================================
// ЗАГОТОВКА. Реализация — Фаза 7 (этап 25), но пригодится и раньше:
// те же примитивы нужны для своих бинарных форматов (карты, сейвы, ассеты).
// =============================================================================
//
// Ручная сериализация примитивов и структур в поток байт и обратно.
// Явный контроль порядка байт и раскладки. Общий код для:
//   - сетевого протокола (shared/net/protocol),
//   - форматов ассетов (Фаза 3),
//   - сейвов (Фаза 6).