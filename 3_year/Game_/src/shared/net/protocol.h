#pragma once

// =============================================================================
// ЗАГОТОВКА. Реализация — Фаза 7 (этап 25). Здесь только назначение модуля.
// =============================================================================
//
// Свой протокол поверх UDP: формат сообщений, версия протокола, типы пакетов
// (подключение, ввод игрока, снапшот состояния, пинг и т.д.).
//
// Общий для клиента и сервера: обе стороны кодируют/декодируют одинаково.
// Никакого memcpy структур в сокет — только явная сериализация (см.
// shared/serialization), чтобы не зависеть от паддинга и выравнивания.
//
// Endianness: Pi 5 (ARM) и x86-ноутбуки оба little-endian, но протокол всё
// равно проектируется с явным порядком байт на будущее.